/* eslint-disable @typescript-eslint/no-unused-vars */
import { GenericTransformPipe } from './generic-transform.pipe';

describe('Pipe: GenericTransforme', () => {
  it('create an instance', () => {
    const pipe = new GenericTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
